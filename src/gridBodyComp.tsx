// STUB — replaced by the full GridBodyComp (GridBodyCtrl + viewport/pinned sections) in T3.3.
// Renders the `.ag-root` element so GridComp's `getFocusableContainers` querySelector resolves.
const GridBodyComp = () => {
  return <div class="ag-root ag-unselectable" role="presentation" />;
};

export default GridBodyComp;
